apiVersion: v1
kind: Service
metadata:
  name: Student-VM-service
spec:
  selector:
    app: Student-VM
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 8090